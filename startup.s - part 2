Vector table and reset-time initialization
bcs 2f
ldr r2, [r0]
str r2, [r1]
adds r0, r0, #4
adds r1, r1, #4
b 1b
/* Zero the BSS before any C/Assembly application code uses it. */
2:
ldr r1, =_sbss
ldr r3, =_ebss
movs r2, #0
3:
cmp r1, r3
bcs 4f
str r2, [r1]
adds r1, r1, #4
b 3b
4:
bl main
/* main must not return in a bare-metal application. */
5:
b 5b
.size Reset_Handler, . - Reset_Handler
.section .text.Default_Handler, "ax", %progbits
.align 2
.type Default_Handler, %function
.thumb_func
Default_Handler:
b Default_Handler
.size Default_Handler, . - Default_Handler
.weak HardFault_Handler
.thumb_set HardFault_Handler, Default_Handler
.section .note.GNU-stack, "", %progbits
